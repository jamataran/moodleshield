node_modules/
.env.local
*.env
*.log
npm-debug.log*

# Datos locales de desarrollo (media, postgres, uploads)
.data/
infra/local/data/
coverage/
.DS_Store

# Ficheros temporales de ffmpeg
*.ts.tmp
*.part

# Editores
.idea/
.vscode/
!.vscode/extensions.json

# Claves generadas en local (las de producción viven en el gestor de secretos)
secrets/
*.pem
!test/fixtures/*.pem

# Notas de trabajo: init.md es el borrador con la necesidad original y el
# volcado de la conversación que originó el proyecto. Se queda en tu disco.
# Si prefieres publicarlo, borra esta línea.
init.md

# Claude: worktrees de agentes y ajustes locales de cada máquina.
# .claude/settings.json (compartido) sí se versiona si algún día existe.
.claude/worktrees
.claude/settings.local.json
